if you don't want to change the original list, use `copy()` and `deepcopy()`

## copy()

copy.copy() will duplicate a mutable value (make a new one, not just duplicate the reference)

## deepcopy()

deepcopy is for lists within lists

